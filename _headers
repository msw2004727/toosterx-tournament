# Cloudflare Pages 快取標頭
# ------------------------------------------------------------------
# 為什麼需要這個檔案：
#
# Pages 預設給靜態資源 `max-age=14400`（4 小時）。index.html 是 max-age=0，
# 所以推版之後 HTML 馬上換新，但 **js/ 與 css/ 會繼續用瀏覽器裡的舊檔最多 4 小時**。
#
# 我們沒有打包工具，動態 import 的模組網址（例如 /js/modules/staff/live.js）
# 帶不了版號，於是就會出現「新的 index.html 配舊的模組」這種混版狀態——
# 實機上表現為賽務台「載入失敗」，以及連線正常卻顯示「資料來自手機快取」。
#
# 現場的意義更嚴重：比賽當天早上推一個修正，賽務手機可能到中午才吃到，
# 中間還是新舊混用。所以這裡一律改成「每次都向伺服器確認」。
#
# 代價很小：有 ETag，沒改就是 304，只有幾百 bytes。
# 真正的離線效能由 Service Worker 的 app shell 快取負責，不靠 HTTP 快取。
#
# ⚠️ 這個檔案是對的，但**在自訂網域上目前無效**（2026-09-02 實測）：
#
#      feda-cup-demo.pages.dev  → public, max-age=0, must-revalidate, no-cache  ✅
#      cup-demo.toosterx.com    → public, max-age=14400, must-revalidate        ❌
#      cup.toosterx.com         → public, max-age=14400, must-revalidate        ❌
#
#    自訂網域前面那層 Cloudflare zone 的 Browser Cache TTL（4 小時）會把
#    **整條 Cache-Control 換掉**——不是只改 max-age 的數值。
#    試過多送一個 no-cache 想蓋過去，沒有用：cf-cache-status: MISS（確定是
#    新鮮回源）、內容也是新版，但 no-cache 一樣被剝掉。`must-revalidate`
#    是 Cloudflare 自己補的，不是這裡送出去的那個。
#    .json 與 HTML 逃過一劫，只是因為不在 Cloudflare 預設會快取的副檔名清單裡。
#
#    → 解法只有一個，而且在後台：toosterx.com zone → Caching → Configuration
#      → Browser Cache TTL 改成 **Respect Existing Headers**。
#      （或針對 cup*.toosterx.com 的 /js/*、/css/* 建一條 Cache Rule。）
#    下面的 no-cache 先留著：語意上本來就該送，zone 設定改好之後它就會生效。
#    驗證：curl -sI https://cup-demo.toosterx.com/js/config.js | grep -i cache-control

/js/*
  Cache-Control: public, max-age=0, must-revalidate, no-cache

/css/*
  Cache-Control: public, max-age=0, must-revalidate, no-cache

/sw.js
  Cache-Control: public, max-age=0, must-revalidate, no-cache

/manifest.json
  Cache-Control: public, max-age=0, must-revalidate, no-cache

# ⚠️ 圖片**不能**長快取，原因不是圖片會變，是「檔案不存在時會發生什麼事」：
#
#    Cloudflare Pages 對找不到的路徑會回 **200 + SPA fallback 的 index.html**，
#    不是 404。這條規則於是把那份 HTML 當成 /img/xxx.png 的正確答案，
#    存進邊緣快取七天。
#
#    2026-09-03 實地發生：manifest 指到 /img/icon-192.png 但 img/ 是空的，
#    有人（或瀏覽器）先打了一次，邊緣就記住了 index.html。
#    隔天圖檔補上去、原站回的是正確的 PNG，
#    **自訂網域仍然回 text/html 整整一週**——
#    Chrome 拿到 HTML 當圖示，判定 manifest 無效，安裝選項照樣不出現。
#
#      feda-cup-demo.pages.dev        → image/png       ✅ 原站是對的
#      cup-demo.toosterx.com          → text/html       ❌ Age: 93586，HIT
#
#    改成每次回源確認就沒有這個問題。代價幾乎是零：四個圖示合計 14KB，
#    只在安裝與加到主畫面時才抓，而且有 ETag，沒改就是 304。
#    真正需要長快取的大圖（球員照）走 Firebase Storage，不在這裡。
/img/*
  Cache-Control: public, max-age=0, must-revalidate, no-cache
