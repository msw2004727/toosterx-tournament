# 13｜驗收整合報告（2026-09-06）

| | |
|---|---|
| **驗收日期** | 2026-09-06 |
| **驗收人** | Claude。流程：讀兩份外部報告 → 逐條在現行程式碼與 demo 站核實 → 修正 → 補測試與變異 → 重跑全部套件 → 部署 demo 與正式站 |
| **核實的基準** | `b26ba3c`（demo，版號 0.20260905h）。兩份外部報告的基準是 `337d503`／`adda1bf`，都比這個舊 |
| **修正後的版本** | 版號 **0.20260906**（demo 與正式站；commit 用 `git log --oneline --grep 0.20260906` 找）|
| **整合的來源** | ① 《驗收計畫書整合版》：146 項、缺陷 D-01…D-15 ② 《Codex 獨立唯讀驗收報告 2026-09-05》：重新分類 146 項、修正 D-01／D-04／D-07／D-09／D-11 的敘述、新增 C-01 |
| **這一份的性質** | **不是唯讀**。每一條缺陷都在現行程式碼上核實，屬實的就修掉、補會紅的測試、補變異，然後部署 |

---

## §1　一句話結論

> **兩份報告指出的 16 條缺陷，15 條在現行程式碼上仍然屬實（只有 D-05 已經不成立），現在全部修掉、每一條都有會紅的測試守著，而且已經部署到 demo 與正式站。** 整合版的方向對、細節有六處要修正；Codex 的修正全部正確，而且多抓到一條（C-01）。兩份都沒有跑過任何一套測試——這一份跑了，包含兩套變異；而且修完在 demo 站實地走一次，又多抓到一條藏在 D-01 後面的缺陷（D-01b）。

修正後的關卡（都在版號 0.20260906 的程式碼上跑）：

| 關卡 | 結果 |
|---|---|
| `npm run test:unit` | ✅ 1150 全綠（47 個 suite，較上一版 +33）|
| `npm run test:e2e` | ✅ 1131 全綠（mobile／desktop／320px 三種寬度，較上一版 +63）|
| `npm run test:mutation` | ✅ 297 / 297 全被抓到（第一次跑 295／296，#C10 補了 T58-G 之後重跑；D-01b 之後再完整跑一次） |
| `npm run test:mutation:e2e` | ✅ 32 / 32 全被抓到（D-01b 之後再完整跑一次） |
| `npm run check:anchors` | ✅ 400 條錨點全部對得上 |
| `npm run test:rules`／`test:fn`／兩套對應的變異 | 沒有重跑。這一輪沒有改 `firestore.rules` 與 `functions/`（`functions/engine/` 是 `js/engine/` 的同步產物，多了 `result.js`，管線沒有用到它）|

---

## §2　對兩份外部報告的評價

### 《驗收計畫書整合版》

**對的地方**：15 條缺陷裡 14 條的根因判斷正確；「哪些會讓現場停擺」的分級（檢錄台、登入頁、攤位作廢、攤位 0 分）是對的。「最值得肯定的五件事」逐一核對過都成立——離線三態、rules 擋直接寫入、同分不隨機、遮蔽依年齡、PK 只在平手時決勝（後者要加一句：**只有管理端對**，見 C-01）。

**要修正的地方**（Codex 已指出，我複核後全部同意）：

| 項 | 原文 | 核實結果 |
|---|---|---|
| 統計 | 儀表板 120／6／9／11 | 逐列重加是 124／6／7／9，儀表板跟明細對不起來 |
| D-01 | 列了三行要改 | 還有兩處（取名單的 `teamId`、寫檢錄的 `teamId`）也讀錯欄位，只改三行仍然壞 |
| D-04 | 「攤位送不出 0 分」 | 只有**現場代建**那一路沒歸零；查既有玩家早就歸零了 |
| D-05 | 「排行榜與抽獎資格完全沒產生」，列三種猜測 | 是**部署落差**（當時雲端還是舊的 `onAttemptCreated`），不是程式壞掉；而且**現在已經不成立**（§3）|
| D-07 | 建議改 `describeEvent` | 那個函式不存在，真正的是 `eventText`；照原文改會改到空氣 |
| D-09 | 「單按抽籤就留下無效稽核」 | 抽籤只改本機草稿、**不寫任何東西**；稽核在真的產生賽程時才寫。缺陷仍在（按鈕沒守衛），但後果描述不實 |

### 《Codex 獨立唯讀驗收報告》

**全部正確**：上表六處修正都經我複核；C-01（賽務端完賽與管理端改判各算一份勝負，PK 標記會分岔）用「正規 3:1、PK 2:4」重現成功。把 146 項改成「確認／程式支持／部分／缺陷／未驗」五類、明說「47 項確認不能換算成通過率」，是比整合版誠實的計法。

**它刻意沒做的**：沒有寫入任何資料、沒有跑任何測試套件、沒有驗證修法。所以它「不建議簽核」在當時是對的——這一份就是把那些缺口補上。

---

## §3　逐條核實與處置

判定欄：**屬實**＝在 `b26ba3c` 上重現或程式證實；**部分屬實**＝現象存在但範圍或敘述要縮小；**已不成立**＝現行版本沒有這個問題。

| ID | 判定 | 核實（在 b26ba3c 上）| 修法 | 守著它的測試 |
|---|---|---|---|---|
| **D-01** 檢錄台不能用 | 屬實 | `checkin.js` 五處讀頂層 `homeTeamId`／`${side}TeamId`／`${side}TeamName`，真實文件是巢狀 `home.teamId`。E2E 替身種子照著錯的 schema 寫，所以一直是綠的 | 五處全改讀巢狀；**兩支 E2E 種子把不存在的欄位拿掉**（不拿掉的話測試證明不了任何事）| checkin.spec 全組（種子已修）、T58-G、變異 #AF13／#E18 |
| **D-01b** 檢錄名單缺複合索引（實地驗證新發現）| 屬實 | D-01 修好、部署到 demo 之後用總管帳號開檢錄台：名單查詢**第一次真的執行**就回 `failed-precondition`——`members(status, jerseyNo)` 的複合索引從來沒有建過，模擬器與替身都不查索引；而畫面把「讀不到」顯示成「這一隊還沒有公開名單」| 補索引（demo 與正式站都已部署並 READY）；檢錄台讀不到時明說原因（缺索引／沒權限／離線無快取）並要求先不要完成檢錄；替身的 `__FAKE_SNAPSHOT_FAIL` 也套用在一次性查詢、可用 `field` 限定缺的是哪一組欄位 | checkin.spec D-01b、T58-G D-01b ×2、#AF16／#E32。demo 實測：U10-G-A-01 兩隊各 8 名球員的生日與後四碼都印得出來 |
| **D-02** 登入頁 TDZ | 屬實 | `const off = onAuth(cb)` 的回呼是**同步**呼叫的，已登入時 `off()` 撞到暫時死區；`onAuth` 又沒接例外 | 先 `if (user()) { navigate(next); return; }` 再訂閱；`onAuth` 的首次回呼包 try/catch（一個壞掉的訂閱者不該讓所有人的 auth 監聽跟著壞）| audit-fixes.spec D-02、T58-G、#AF10／#E19 |
| **D-03** 攤位無法作廢 | 屬實 | `firestore.indexes.json` 沒有 `attempts(staffUid ASC, createdAt DESC)`；`booth.js` 兩支 onSnapshot 的 onError 是 `() => {}` | 補索引（已部署 demo 與正式站）；讀不到時畫面明說「讀不到最近登錄，送出的成績仍然有效，只是這裡沒辦法作廢」；替身 SDK 新增 `__FAKE_SNAPSHOT_FAIL` 讓這種錯誤在 E2E 裡模擬得出來 | audit-fixes.spec D-03、T58-G（索引 ↔ 查詢逐欄比對）、#AF11／#E21 |
| **D-04** 攤位送不出 0 分 | 部分屬實 | 只有 `createOnSite()` 沒把 stepper 歸零；`lookup()` 早就有 | 抽成 `resetInput()`，兩條路共用 | audit-fixes.spec D-04（代建 → 直接送 0 分 → `rawValue` 是 0）、#E20 |
| **D-05** 排行榜沒產生 | **已不成立** | `firebase functions:list` 在 demo 只有 `onAttemptWritten`，舊的 `onAttemptCreated` 已不在；正式站 23 支 Functions 也是新版（2026-09-05 部署）| 不需要改程式 | 主辦手動驗收攤位時直接看排行榜與抽獎張數 |
| **D-06** 重開後顯示下半場 | 屬實 | `buildReopenPatch` 寫死 `period: 'h2'`；同一支 `lastPlayedPeriod` 沒事件時也退回 `'h2'`（六個組別全是單節）| 重開先讀事件流、退回**最後打過的那一期**；沒有事件流退第一期。三分鐘自撤回共用同一支，所以一起修 | admin-match.spec D-06 ×2、T58-B、T31-4（舊測試的預期改成 h1）、#AF2／#AF6／#E28 |
| **D-07** 事件寫「上半場」 | 屬實 | `eventText` 自己一份 `PERIOD_TEXT`；公開端 `eventLine` 又一份；公開比賽頁對單節組別照樣印「半場 x-y」| 兩邊都改用 `lib/format.js` 的 `periodLabel(period, periods)`；單節不畫半場 | audit-fixes.spec D-07、T58-C、#AF3／#AF15／#E26 |
| **D-08** 隊職員排在前面、可被選為進球者 | 屬實 | Firestore `orderBy('jerseyNo')` 把 null 排最前；出場名單給隊職員畫先發／替補鈕；賽務台 `pickPlayer` 不濾身分 | 資料層 `sortRosterForMatch`（球員依背號、沒背號的球員接著、隊職員最後）；出場名單的隊職員只標身分；選單只列球員 | audit-fixes.spec D-08 ×2、T58-D、#AF4／#AF5／#E29／#E30／#E31 |
| **D-09** 已開打仍可抽籤 | 部分屬實 | 抽籤鈕只看 `busy`，`doDraw` 沒守衛。但抽籤**不寫任何東西** | 按鈕反灰＋畫出原因；`doDraw` 本體再擋一次 | admin-schedule.spec D-09、T58-G、#E22 |
| **D-10** 取消／延期後可重產 | 屬實（含延期）| `canRegenerate` 只看狀態；延期／取消不清比分，打完才取消的場次 result 還在卻被放行 | 新增 `hadResult()`：狀態、result、revisionCount、lock 任一成立就算打過；賽程頁凍結號碼、鎖定列、統計三處一併改用 | admin-schedule.spec D-10 ×2、T58-F、#AF8／#AF9 |
| **D-11** `walkoverSide` 殘留 | 屬實 | 改判比分與重開都不動 `walkoverSide`，文件會同時說「客隊棄賽」與「1:1 平手」| 改判非棄賽時明確寫 `null`（原本就是棄賽的維持）；重開也清 | admin-match.spec D-11、T58-B／T58-E、#AF7 |
| **D-12** 棄賽鈕反灰不說原因 | 屬實 | `canWalkover` 有原因，畫面只拿 `ok` 去 disabled | 畫出 `woG.reason` | admin-match.spec D-12 ×2、#E23 |
| **D-13** 賽務首頁三顆鈕不分角色 | 屬實 | `toolsBar()` 沒問 `can()`；攤位人員按「檢錄」進到「你沒有檢錄權限」| 「檢錄」看 `checkin.write`、「出場名單」看 `matchsheet.write`。**「進入賽務台」保留給所有賽務身分**——那一頁對裁判與檢錄員是唯讀的時鐘與比分，Codex 提醒的唯讀用途我同意 | audit-fixes.spec D-13 ×3、T58-G、#E24 |
| **D-14** 404 標題不更新 | 屬實 | not-found 分支在設 `document.title` 之前就 return | 補上 | audit-fixes.spec D-14、#AF14／#E25 |
| **D-15** 觸控目標 < 44px | 屬實 | 主題鈕 34px 高（Codex 量到 29×34）；積分榜隊名是一顆沒有高度的按鈕；頁首連結沒有寬度下限（Codex 量到 21×44）；demo 的「切換身分」30px 高 | 主題鈕 44×44；隊名按鈕 44 高；頁首連結最小 44 寬；切換身分 44 高 | audit-fixes.spec D-15（真的量 boundingBox）、#E27 |
| **C-01** PK 標記兩份實作 | 屬實 | `buildFinishPatch` 用 `pk ? 'penalty' : 'regulation'`；管理端 `resultOf` 才是對的 | 新增 `js/engine/result.js` 的 `matchResult()`，**兩邊都從這裡拿**（R-ENG-001）；原本的 #MA2／#MA3 變異錨點移到引擎 | T58-A、#AF1、#MA2／#MA3 |

### 順手一起處理的

- `js/modules/challenge/board.js` 兩個空的錯誤回呼改成 `console.warn`（Codex L7 提到的空 handler）。
- `CLAUDE.md` 的角色表寫著裁判 2.6／攤位 2.2，`ROLE_INFO` 其實是攤位 2.1／檢錄 2.2／裁判 2.3／記錄員 2.4。改正，並加一條單元測試把表格跟 `ROLE_INFO` 對起來（變異 #AF12）。
- 出場名單的隊職員列加了淡化樣式，一眼看得出不是球員。
- **變異 #C10 差點失守**：賽務首頁多讀了一次 `matchsheet.write` 之後，出場名單頁改讀別的權限（#C10）在 T42-8「有人讀」底下就逃掉了——第一次跑單元變異是 295／296。T58-G 現在另外釘住「權限碼要由擁有那個功能的頁面自己讀」。這正是 CLAUDE.md 講的「某一層有測 ≠ 變異抓得到」。

### 沒有改、而且說明為什麼

- **D-10 的子集合**：整合版建議重產時一併刪 `timeline`。Codex 認為那涉及歷史資料與留痕、不宜在這一輪做，我同意——`hadResult()` 讓打過的場次根本進不到重產，子集合的問題就不會發生。
- **`.btn--sm` 的 36px**：Codex 引用的 `components.css:32` 是小型按鈕的通用尺寸，用在列表裡的次要動作。全部拉到 44 會讓管理後台的表格列變高一截，這一輪只改他量到不合格的那幾個元件。
- **賽務台入口**（D-13 的第三顆鈕）：見上表。

---

## §4　這一輪的規模

| | 數量 |
|---|---|
| 改動的原始碼檔案 | 22（含新引擎檔 `js/engine/result.js`）|
| 新增的單元測試 | 33（`tests/unit/audit-fixes.test.js`，T58-A…G）|
| 新增的 E2E | 21 條 × 3 種寬度（`tests/e2e/audit-fixes.spec.js` 12 條、admin-match 5 條、admin-schedule 3 條、checkin 1 條）|
| 新增的變異 | 單元 16 條（#AF1–#AF16）＋ E2E 15 條（#E18–#E32）|
| 更新的變異錨點 | 3 條（#17、#MA2、#MA3，因為程式碼搬家）|
| 部署 | demo 與正式站：rules＋兩筆新索引（attempts、members）、前端（兩站都是 0.20260906）|

---

## §5　在 demo 站實地走過的

用主辦的總管帳號（Chrome 既有的 LINE 登入）在 cup-demo.toosterx.com 0.20260906 上：

| 項 | 結果 |
|---|---|
| D-02 已登入開 `#/login` | 直接落到 `#/my`，沒有任何錯誤 |
| D-01 檢錄台 `U10-G-A-01` | 隊名「龍井白鯊 vs 大甲金剛」正確；**第一次**兩隊都 0/0 且主控台印 `The query requires an index`（就是 D-01b）；補索引、等它 READY 之後重新整理：兩隊各 8 名球員、生日與後四碼都在，兩位隊職員排在最後、進度分母只算球員（0／8）|

## §6　仍然沒有人驗過的（跟 Codex §六 對齊）

這些是**這一輪也沒做**的，不是「做了沒過」：

1. **主辦要手動驗收的挑戰攤位**：真的送成績 → isBest → 排行榜 → 完成關卡 → 抽獎張數 → 作廢回退 → 超額加場。D-03／D-04 修好之後這條線在 E2E 是綠的，但 Functions 的重算延遲只有真站量得到。
2. **真手機**：LINE 授權、掃 QR、iOS Safari 的安裝教學、現場網路。
3. **離線與恢復**在真手機上的行為（E2E 的離線是替身模擬的）。
4. **多人同時操作**與現場效能。
5. **彩排（10/6–10/7）**本身。

正式站上還等主辦決定的事，跟上一份回報一樣：報名截止 9/24 00:00 是否要提前到彩排前。

---

## §7　怎麼複核這一份

```bash
git checkout demo && git pull
npm run test:unit          # 1150
npm run test:e2e           # 1131
npm run test:mutation      # 單元層變異，約 75 分鐘
npm run test:mutation:e2e  # 畫面層變異，約 30 分鐘
npm run check:anchors      # 兩秒
```

要重現任何一條原本的缺陷：`git checkout b26ba3c`，本機開站（本機一律連 demo）。

---

## §8　補驗（2026-09-06 晚間）：球隊報名、審核、登記球員

主辦要求：在瀏覽器裡驗收報名相關系統，有問題修正，然後在入口頁加圖文教學彈窗。

### 8.1　怎麼驗的

不用替身，直接對 **cup-demo.toosterx.com** 用 Playwright 開四個獨立的瀏覽器
（訪客、隊長、隊友、管理員；隊長與隊友用 demo 的「切換身分」拿匿名帳號），
走完整條線並在每一步截 390px 寬的圖：

| 段 | 走了什麼 | 檢查 |
|---|---|---|
| 0 | 訪客看 #/register、按建隊被帶去登入 | 4 |
| 1 | 隊長建成人組球隊、拿邀請碼 | 8 |
| 2 | 隊友：未登入開邀請連結、錯的碼、填資料送申請 | 7 |
| 3 | 隊長同意、送出報名、名單凍結、加入頁收表單 | 8 |
| 4 | 管理員核准（名單檢核）、隊長看到已通過、公開名單、「我的」 | 9 |
| 4b | 已通過的隊長申請取消 → 管理員取消並記退費 | 5 |
| 5 | 學童組：教練填名單（超齡擋下、隊職員不填生日、人數上限）、送出、撤回、再送 | 14 |
| 6 | 管理員退回（填原因）→ 教練看到原因、修改 → 再送 → 核准 → 公開名單用暱稱 | 9 |
| 7 | 稽核紀錄 | 1 |

第一輪 **44 / 50**；修正並部署規則後第二輪 **64 / 65**（剩下那一條是還沒部署到
demo 的前端修正，本機 E2E 綠）；最後一輪在部署後跑，見 8.4。瀏覽器 console 零錯誤。

### 8.2　抓到並修掉的

| # | 嚴重度 | 缺陷 | 為什麼 E2E 沒抓到 | 修法與守衛 |
|---|---|---|---|---|
| R-A | **高** | **被退回的球隊送不出去。** 規則只放行 rejected → draft，管理頁的「送出報名」寫的是 submitted。本機快照先顯示「待主辦審核」，下一秒被規則打回來，畫面只說「可能是報名已經截止」 | 替身沒有規則；`test:rules` 原本沒有這一條轉換 | `firestore.rules` 放行 rejected → submitted；R98c／R98d；變異 RU#46 |
| R-B | 中 | **教練表單驗證失敗會清掉打到一半的民國年。** 只填年就按「加入名單」，「請填出生年月日」出現的同時「106」不見了 | 沒有一條測試在失敗之後看欄位 | `manage.js` 的 `rocDateInput` 帶 `parts`；E2E R-1；變異 #E36 |
| R-C | 低 | 學童組管理頁在組別讀到之前先閃一下「邀請隊友」再換成「新增球員」 | 快照與 getDoc 的時序 | 組別讀到之前放骨架；E2E R-2 |
| R-D | 低 | `join.js` 自己定義了一份 `isYouthDivision`（看 scorerBoard），跟管理頁（看年齡門檻）是兩份 | 種子資料兩者一致 | 改用引擎那一份 |

其餘 60 項檢查通過：狀態機（草稿→待審→已通過／已退回→再送→已通過、撤回、取消退費）、
名單凍結、規章第十二條人數上限、超齡擋下並寫出門檻、暱稱不遮、公開名單看不到後四碼與生日、
審核頁的名單檢核與退回原因、稽核留痕。

### 8.3　圖文教學彈窗

入口頁 `#/register`：第一次進來自動跳（報名關閉時不跳），關掉就記住；
「看圖文教學」與步驟卡底下的兩顆按鈕隨時可再開；`#/register?guide=youth` 直接開學童組。
成人組 9 步、學童組 9 步，每一步是真站截圖（390×560）加 SVG 標記框與編號、上方 SVG 進度圖。
以 390px 為主、320px 驗過沒有橫向捲軸、深色主題與桌面也看過。
守衛：單元 T63-1～T63-8（內容、圖檔存在、PNG 尺寸、標記框在範圍內），E2E 8 條
（自動跳一次、關閉時不跳、兩條流程每一步的圖都載得出來、最後一步帶去登入、
`?guide=youth`、320px、換頁收掉）。

### 8.4　最後一輪與清理

部署到 demo 後再跑一次同一支腳本（見 §8.5 的結果），之後用 Admin SDK 刪掉
測試建立的球隊（含 members／roster 子集合）與匿名身分；稽核紀錄依 R-SEC-002 留著。

### 8.5　結果

部署 0.20260906h 到 demo 之後跑最後一輪：**70 / 70 通過、瀏覽器 console 零錯誤**
（比第二輪多了 5 項：教學第一次自動跳、圖載得出來、下一步、關掉後不再跳、入口按鈕）。
原本第二輪剩下的那一條（打到一半的年份被清掉）在真站上也過了。

之後用 Admin SDK 刪掉三輪測試建立的球隊（含 members／roster）與匿名身分，
並確認 demo 的 teams 集合裡沒有「台中黑豹」「台中小飛達」殘留；稽核紀錄留著（R-SEC-002）。

用瀏覽器面板（手機視窗）再看一次 cup-demo 的 `#/register?guide=1`：彈窗、分頁、
進度圖、截圖與標記、上一步下一步、最後一步的「我要建立球隊」、Esc 關閉、
localStorage 記住，全部如預期。

本機：單元 1166（49 suite）、E2E 1155（一條 admin-teams 320px 的橫向捲軸案例偶發逾時，
重跑兩次全過）、rules 230（新增 R98c／R98d）、變異 RU#46 與 #E36 見 §7 的指令。

---

## §9　第二輪驗收單核實（2026-09-06 晚間，測試人：張貽鈞）

主辦交來兩張驗收單（家長／隊長 12 項、管理員 10 項），指示「先審核確認是否屬實再修正，
不是問題的只回報」。逐條對照**正式站與 demo 的資料庫**與程式碼核實如下。

> ⚠️ 兩張單都寫「環境：demo」，但測試人貼的網址是 **cup.toosterx.com**，正式站資料庫裡
> 也真的留下了 8 支測試球隊（測試、Demo、Demo2、Demo3、雷兒TEST ×2、雷兒TEST3、測3）、
> 3 場已完賽的 U10 場次、一份人工裁定過的積分榜，而且 U10 的賽程是**已發布**的——
> 公開端現在看得到這些假隊伍與比分。清理腳本已備好（`scratchpad/prod-cleanup.mjs`，
> 預設 dry-run），**等主辦確認再執行**。

### 9.1　家長／隊長

| 項 | 測試人的描述 | 核實 | 處置 |
|---|---|---|---|
| R-3 | 「用邀請碼報名了 但是」（句子沒寫完） | 資料庫：同一個帳號對同一隊連送 5 筆申請，只有第一筆留著，其餘被 Function 退件（同一帳號一次只能有一筆待審，docs/10 §3.3）。**規則是對的，但申請人完全沒有得到任何回饋** | 屬實（缺回饋）：加入頁先講「你已經有一筆待審」；送出後盯著那一筆，被退件就把原因留在頁上；「我的」的球員列印出退件原因 |
| R-5 | 邀請連結「跟網址不太一樣」 | 操作誤會：LINE 內建瀏覽器的網址列帶著 `?lineAppVersion=…`，邀請連結是 `/#/join/代碼`，兩者本來就不同。但 LINE 內建瀏覽器常常不給自動複製 | 不算缺陷；順手把連結印在卡片上（可長按選取）並加「分享給隊友」（手機分享面板，LINE 就在裡面） |
| R-6a | 「後四碼 1234 可以、9070 不行」 | 資料庫：9070＋同一個生日的孩子已經在另一支球隊待審，四筆都被「每人限報乙隊」退件。**退件在名單頁上是看不見的**（那一列直接消失），測試人才會猜是數值太大 | 屬實：名單頁列出系統退件的成員與原因，隊長可收掉（rules R57c、RU#49） |
| R-6b | 球隊頁要有隊長編輯／審核的連結 | 屬實：「我的」連到公開球隊頁，而那一頁沒有任何路通往管理頁 | 「我的」直接進管理頁；公開球隊頁隊長看得到「管理名單／審核申請」 |
| R-6c | 教練名單顯示背號、背號不可重複 | 屬實：隊職員表單也問背號（正式站的領隊掛著 20 號）；撞號只在主辦審核時才擋 | 隊職員不問背號、存 null；球員撞號在「加入名單」時就擋 |
| R-11 | 球隊頁沒有申請取消鈕 | 部分屬實：已送出／已通過的管理頁有「申請取消報名」，**草稿與被退回的沒有任何取消的路** | 草稿由隊長自己取消（rules R98e／R98f、RU#47／#48）；被退回的也有「申請取消」 |
| 比分 | 同分發生 PK 時要有欄位 | 屬實：公開端的比賽頁與賽程列不印 PK 比分 | 比賽頁與賽程列加「PK 4-3」 |
| 稽核 | 要有同分人工裁定名次的紀錄 | 屬實（紀錄其實在，只是印成 `standing.manualRanking：積分榜 u10__group__A`，而且不在任何分類；正式站 87 筆裡 62 筆都是這樣） | 見 9.2 M-7 |
| 稽核 | 文字斷行位置不好 | 屬實：360px 的手機上四字標籤折成「報名審／核」 | 分頁列不換行、橫向捲動 |
| R-9 備註 | 未報名成功的原因要在按鈕下方 | 同 R-6a | 系統退件的原因列在名單區；加入頁的原因留在頁上 |

### 9.2　管理員

| 項 | 測試人的描述 | 核實 | 處置 |
|---|---|---|---|
| M-3a | 安卓手機設定時間不是 24 小時制 | 不是程式的錯：`<input type="time">` 的原生選擇器跟著手機的 12／24 小時設定走，網頁改不掉 | 改成文字格：打 `930`、`9:30`、`14.30` 都收，存 24 小時制 `HH:MM`（`normalizeHHMM`） |
| M-3b | 好像不能手動對調隊伍 | 操作誤會＋介面沒說：demo 上成人公開組已經有場次開打，分組本來就不能再動（R-SCHED-002），但按鈕沒有關、也沒有寫原因，點了會對調、產生對戰卻被擋；U10 與女子組是單一組別，本來就沒有分組可調，按鈕是灰的但一個字都沒說 | 有場次開打就把分組按鈕關掉並寫出原因；單一組別寫「只有一組，沒有分組可調」（部署後在 demo 逐組核對過：U6／U8／興趣仍可對調） |
| M-4 | 總比分沒加上 PK 比分 | 屬實：場次改判頁的頁首只寫狀態，沒有目前比分與 PK | 頁首加「目前比分 2:2（PK 4:3）」 |
| M-5 備註 | 申訴成立是不是要先改比分 | 建議：裁決與改判是兩個動作（成立也可能只是紀律問題），不強制 | 申訴成立之後多一顆「到改判比分」的捷徑 |
| M-6 備註 | 改完比分按返回跳到「我的」 | 屬實：後台每一頁的返回鍵都固定回「我的」 | 從別頁點進來就回那一頁，直接用網址開的才回「我的」 |
| M-7 | 少了同積分人工裁定名次的紀錄 | 屬實（紀錄在，但印成代碼、不在分類裡） | 稽核引擎補 16 種動作的人話（積分裁定、賽程、申訴、直播、報名開關、系統退件、取消退費、匯出）；分類改成 7 個，「其他」是補集，加總等於全部 |
| M-9.1 | 家長手機第二個小孩的編號在哪新增 | 設計如此（攤位代建）但不好找：原本要自己想一個沒人用的代號打進去、查不到再代建 | 攤位頁加「代建新卡（系統配號）」 |
| M-9.2 | 左上角「已連線」白字看不到 | 屬實：三態燈的白字是為賽務端的深色頁首設計的，攤位頁直接放在淺色底上 | 三態燈預設跟著主題走，只在深色頁首裡才用白字 |

順手：審核頁對「比賽當天不到 3 歲」的球員提醒生日填錯（正式站真的收到 5 筆 2026-09-06 出生的成人，
是原生日期選擇器預設「今天」造成的；加入頁的生日也改成民國年三格）。

### 9.3　守衛

單元：T39-D（稽核人話與分類）、T-review（生日填錯）、T53-R（退件原因）、T-sched（24 小時制）；
rules：R98e／R98f（草稿取消）、R57c（系統退件可收掉）；
E2E：register 10 條、my-home 2 條、admin-audits 3 條、admin-match 4 條、admin-schedule 2 條、booth 2 條、public-site 4 條；
變異：#AF18–#AF22、#E37–#E47、RU#47–#49（`register.spec.js` 加進 E2E 變異的執行清單）。
本機重跑結果：E2E 變異 **47 / 47**（#E44 第一次逃掉，測試改用 6 隊兩組範本後抓到）、rules 變異 **48 / 48**；
單元變異（#AF18–#AF22）由 CI 跑。

### 9.4　補：攤位 B-5「作廢時沒有跳出原因視窗」

| 項 | 核實 | 處置 |
|---|---|---|
| B-5 | **屬實**。docs/06 §6.3 寫「作廢＝voided + 原因」，驗收表 B-5 也寫「按作廢，填原因」，但攤位頁只跳確認框，原因寫死成「攤位作廢」，沒有問過人 | 確認之後多一步選原因（掃錯玩家／成績輸錯／其他自己填），沒選就不作廢；原因寫進 `voidReason`。E2E C07 改成連原因一起驗，變異 #E47 |
