# 行尾一律 LF —— 包含 Windows 的工作目錄
# ------------------------------------------------------------------
# 為什麼要釘死（2026-09-03 出過事）：
#
# 全域 core.autocrlf=true 會讓 Windows 上的 `git checkout` 把檔案換成 CRLF。
# `git status` 看起來乾淨（git 比對時會正規化），但檔案在磁碟上真的變了。
#
# 變異測試（scripts/mutation-*.cjs）用的是**逐字的多行字串比對**：
# CRLF 之後單行的樣式還對得上，多行的全部對不上——27 條變異有 18 條
# 變成「找不到要變異的程式碼」，而那看起來像是腳本過期，不像環境問題。
#
# CI 跑 Linux（LF），所以這種差異只會在某些人的機器上出現。
* text=auto eol=lf

# 二進位檔不要動
*.png binary
*.jpg binary
*.ico binary
*.pdf binary
